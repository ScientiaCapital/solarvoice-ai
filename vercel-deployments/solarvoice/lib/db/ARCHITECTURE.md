# IndexedDB Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     SolarVoice AI Platform                      │
│                    (Next.js 15 + TypeScript)                    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        React Components                         │
├─────────────────────────────────────────────────────────────────┤
│  AgentMarketplace  │  VoiceInterface  │  DashboardPanel        │
│  ─────────────────────────────────────────────────────────      │
│  useHybridAgents() │ saveVoiceInteraction() │ useSyncStatus() │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                          React Hooks                            │
│                         (lib/db/hooks.ts)                       │
├─────────────────────────────────────────────────────────────────┤
│  • useAgentCache()        - Filtered agent browsing            │
│  • useCachedAgent()       - Single agent lookup                │
│  • useVoiceHistory()      - Interaction tracking               │
│  • usePendingActions()    - Background sync queue              │
│  • useSyncStatus()        - Real-time monitoring               │
│  • useAgentSync()         - Manual sync trigger                │
│  • useHybridAgents()      - Cache-first pattern ⭐            │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     IndexedDB Operations                        │
│                     (lib/db/indexeddb.ts)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Agent Cache (12 functions)                                    │
│  ├─ cacheAgentConfigs()      - Bulk store from API            │
│  ├─ getCachedAgents()        - Filtered retrieval             │
│  ├─ getCachedAgentBySlug()   - Single lookup                  │
│  ├─ toggleAgentFavorite()    - Mark favorite                  │
│  └─ updateAgentLastUsed()    - Track usage                    │
│                                                                 │
│  Voice History (5 functions)                                   │
│  ├─ saveVoiceInteraction()   - Store interaction              │
│  ├─ getRecentInteractions()  - Query history                  │
│  ├─ getVoiceStats()          - Aggregate stats                │
│  └─ pruneVoiceHistory()      - Cleanup old data               │
│                                                                 │
│  Pending Actions (8 functions)                                 │
│  ├─ queuePendingAction()     - Queue for sync                 │
│  ├─ getPendingActions()      - Get all pending                │
│  ├─ markActionSyncing()      - Mark as syncing                │
│  ├─ markActionFailed()       - Handle failures                │
│  └─ removePendingAction()    - Delete after sync              │
│                                                                 │
│  Sync Status (4 functions)                                     │
│  ├─ updateSyncStatus()       - Update state                   │
│  ├─ getSyncStatus()          - Get entity status              │
│  └─ getOverallSyncStatus()   - Aggregate status               │
│                                                                 │
│  Utilities (4 functions)                                       │
│  ├─ clearAllData()           - Logout cleanup                 │
│  ├─ getDbStats()             - Debug info                     │
│  ├─ healthCheck()            - Verify operational             │
│  └─ exportAllData()          - Data export                    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Dexie.js Database                         │
│                    (IndexedDB Abstraction)                      │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Browser IndexedDB Storage                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Database: "solarvoice_ai" (Version 1)                         │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Table: agentConfigs                                      │ │
│  │  Purpose: Agent marketplace cache                        │ │
│  │  Primary Key: slug (string)                              │ │
│  │  Indexes: trade, scale, syncedAt, isFavorite, lastUsedAt │ │
│  │  Compound: [slug+trade]                                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Table: voiceHistory                                     │ │
│  │  Purpose: Voice interaction tracking                     │ │
│  │  Primary Key: id (auto-increment)                        │ │
│  │  Indexes: agentId, timestamp, userId, sessionId, status  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Table: pendingActions                                   │ │
│  │  Purpose: Background sync queue                          │ │
│  │  Primary Key: id (auto-increment)                        │ │
│  │  Indexes: type, status, createdAt                        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Table: syncStatus                                       │ │
│  │  Purpose: Sync state tracking                            │ │
│  │  Primary Key: id (entity name)                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

### Hybrid Cache-First Pattern (Recommended)

```
User Request
    │
    ▼
┌──────────────────────┐
│  useHybridAgents()  │
└──────────────────────┘
    │
    ├─────────────────────────────────────────┐
    │                                         │
    ▼                                         ▼
┌──────────────────────┐              ┌──────────────────────┐
│  Check Cache First   │              │  Background Sync     │
│  getCachedAgents()   │              │  (async)             │
└──────────────────────┘              └──────────────────────┘
    │                                         │
    ▼                                         ▼
┌──────────────────────┐              ┌──────────────────────┐
│  Return Instantly    │              │  fetch("/api/...")   │
│  (from IndexedDB)    │              └──────────────────────┘
└──────────────────────┘                      │
    │                                         ▼
    │                                  ┌──────────────────────┐
    │                                  │  cacheAgentConfigs() │
    │                                  │  (update cache)      │
    │                                  └──────────────────────┘
    │                                         │
    └─────────────────────────────────────────┘
                    │
                    ▼
            ┌──────────────────────┐
            │  Component Updates   │
            │  (fresh data)        │
            └──────────────────────┘
```

### Voice Interaction Flow

```
User Speaks → Agent Responds
         │
         ▼
┌──────────────────────────────────┐
│  saveVoiceInteraction()          │
│  {                                │
│    agentId: "residential-solar"  │
│    userInput: "..."              │
│    agentResponse: "..."          │
│    emotion: "enthusiastic"       │
│    duration: 45                  │
│    status: "success"             │
│  }                                │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  IndexedDB: voiceHistory         │
│  (stored locally)                │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  updateAgentLastUsed()           │
│  (track usage timestamp)         │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  getVoiceStats()                 │
│  (aggregate analytics)           │
└──────────────────────────────────┘
```

### Offline Operation Flow

```
Network Disconnected
         │
         ▼
┌──────────────────────────────────┐
│  User Action (create agent)      │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  fetch() → Network Error         │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  queuePendingAction()            │
│  {                                │
│    type: "agent_create"          │
│    payload: { ... }              │
│    status: "pending"             │
│    retryCount: 0                 │
│  }                                │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  IndexedDB: pendingActions       │
│  (queued for later)              │
└──────────────────────────────────┘

         ... Later ...

Network Reconnected
         │
         ▼
┌──────────────────────────────────┐
│  getPendingActions()             │
└──────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  For each action:                │
│  1. markActionSyncing()          │
│  2. fetch("/api/...")            │
│  3. Success → removePendingAction│
│     Failure → markActionFailed() │
└──────────────────────────────────┘
```

## Component Integration Map

```
app/
├── layout.tsx
│   └── initializeDatabase() ← Call on mount
│
├── agents/
│   ├── page.tsx
│   │   └── useHybridAgents() ← Cache-first agent list
│   │
│   └── [slug]/
│       └── page.tsx
│           └── useCachedAgent(slug) ← Single agent lookup
│
├── dashboard/
│   ├── page.tsx
│   │   ├── useVoiceHistory() ← Show stats
│   │   └── useSyncStatus() ← Display sync state
│   │
│   └── favorites/
│       └── page.tsx
│           └── useAgentCache({ isFavorite: true })
│
└── components/
    ├── VoiceInterface.tsx
    │   └── saveVoiceInteraction() ← Track every interaction
    │
    ├── SyncIndicator.tsx
    │   └── useSyncStatus() ← Real-time status
    │
    └── OfflineAlert.tsx
        └── isIndexedDBSupported() ← Check compatibility
```

## Performance Optimization Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                     Performance Layers                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: IndexedDB (Instant)                                  │
│  ├─ Indexed queries: O(log n)                                  │
│  ├─ Primary key lookups: O(1)                                  │
│  └─ Compound indexes: O(log n)                                 │
│                                                                 │
│  Layer 2: Memory Cache (Fastest)                               │
│  ├─ React state management                                     │
│  ├─ Component-level caching                                    │
│  └─ Zustand/Redux for global state                             │
│                                                                 │
│  Layer 3: API Server (Slower)                                  │
│  ├─ Background sync                                            │
│  ├─ Cache updates                                              │
│  └─ Real-time data when needed                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Optimization Rules:
1. Always query from cache first
2. Use indexes for filtering (trade, scale, etc.)
3. Limit result sets (avoid full table scans)
4. Batch operations (bulkPut, bulkDelete)
5. Prune old data regularly
6. Background sync for updates
```

## Security Model

```
┌─────────────────────────────────────────────────────────────────┐
│                     Security Boundaries                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Client-Side (IndexedDB)                                       │
│  ✅ Public agent configurations                                │
│  ✅ User's own voice history                                   │
│  ✅ User's own pending actions                                 │
│  ✅ UI preferences (favorites)                                 │
│  ✅ Cache timestamps                                           │
│                                                                 │
│  Server-Side (API Routes)                                      │
│  🔒 API keys (ELEVENLABS_API_KEY)                             │
│  🔒 Authentication tokens                                      │
│  🔒 Payment information                                        │
│  🔒 User credentials                                           │
│  🔒 Sensitive configuration                                    │
│                                                                 │
│  On Logout:                                                    │
│  🧹 clearAllData() → Delete all IndexedDB data                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Type Safety Architecture

```typescript
┌─────────────────────────────────────────────────────────────────┐
│                     TypeScript Type Flow                        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
                    ┌─────────────────────┐
                    │   AgentType         │
                    │   (from API)        │
                    └─────────────────────┘
                                │
                                ▼
                    ┌─────────────────────┐
                    │ CachedAgentConfig   │
                    │ extends AgentType   │
                    │ + syncedAt          │
                    │ + isFavorite        │
                    │ + lastUsedAt        │
                    └─────────────────────┘
                                │
                                ▼
                    ┌─────────────────────┐
                    │  Dexie Table<T>     │
                    │  (type-safe)        │
                    └─────────────────────┘
                                │
                                ▼
                    ┌─────────────────────┐
                    │  React Hooks        │
                    │  (inferred types)   │
                    └─────────────────────┘
                                │
                                ▼
                    ┌─────────────────────┐
                    │  Components         │
                    │  (type-checked)     │
                    └─────────────────────┘

All operations are fully type-safe from API → IndexedDB → React
```

## File Dependencies

```
indexeddb.ts
├─ Imports:
│  ├─ dexie
│  └─ ../agent-types (Trade, Scale, AgentType)
│
└─ Exports:
   ├─ Types (CachedAgentConfig, VoiceInteraction, etc.)
   └─ Functions (40+ operations)

hooks.ts
├─ Imports:
│  ├─ react (useState, useEffect, useCallback)
│  └─ ./indexeddb (all functions)
│
└─ Exports:
   └─ 7 React hooks

index.ts
├─ Imports:
│  ├─ ./indexeddb
│  └─ ./hooks
│
└─ Exports:
   └─ All operations (barrel export)

__tests__/indexeddb.test.ts
├─ Imports:
│  ├─ @jest/globals
│  ├─ ../indexeddb
│  └─ ../../agent-types
│
└─ Exports:
   └─ 23 test cases
```
